<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.milk.bread.bread_milk">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning"
        tools:targetApi="m"
        android:name=".GlobalClass"
        android:fullBackupContent="true">
        <activity android:name=".activity.LoginActivity"
            android:theme="@style/MyTransparentTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.HomeScreen"
            android:configChanges="orientation|screenSize|keyboardHidden"/>
        <activity android:name=".activity.SubCategoryScreen"/>
        <activity android:name=".activity.MyAccountScreen"/>
        <activity android:name=".activity.EditAccount"/>
        <activity android:name=".activity.ShopByCategoryScreen"/>
        <activity android:name=".activity.CartScreen"/>
        <activity android:name=".activity.DeliveryScreen"
            android:configChanges="keyboardHidden"/>


    </application>


    <supports-screens android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true"
        android:anyDensity="true"
        tools:ignore="ManifestOrder" />
</manifest>